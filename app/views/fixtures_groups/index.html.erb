<h1>Welcome to Top 5 Fantasy Soccer</h1>
<p>Here you can create multiple private and/or public leagues, join multiple leagues, try and predict our selected soccer fixtures, and more.</p>


<% @fixtures_groups.each do |group| %>
  <% if group.status == true %>
    <% game = Unirest.get("http://api.football-data.org/v1/fixtures/#{group.fixture1_id}").body %>
    <h2><%= "#{game["fixture"]["homeTeamName"]} hosts #{game["fixture"]["awayTeamName"]}" %></h2>
    <h3>===========================</h3>
    <% game = Unirest.get("http://api.football-data.org/v1/fixtures/#{group.fixture2_id}").body %>
    <h2><%= "#{game["fixture"]["homeTeamName"]} hosts #{game["fixture"]["awayTeamName"]}" %></h2>
    <h3>===========================</h3>
    <% game = Unirest.get("http://api.football-data.org/v1/fixtures/#{group.fixture3_id}").body %>
    <h2><%= "#{game["fixture"]["homeTeamName"]} hosts #{game["fixture"]["awayTeamName"]}" %></h2>
    <h3>===========================</h3>
  <% end %>
<% end %>