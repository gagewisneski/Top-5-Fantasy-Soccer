<h1>All public leagues:</h1>
<% @leagues.each do |league| %>
<div>
  <h2><%= link_to league.name, "/leagues/#{league.id}" %>: <%= league.users.count.to_s %> Players</h2>
  <% users = league.users.order(score: :desc) %>
  <% users.each do |user| %>
    <div>
    <h4><%= "#{user.name} Score: #{user.score}" %></h4>
    </div>
  <% end %>
  <h5>===================</h5>
<% end %>